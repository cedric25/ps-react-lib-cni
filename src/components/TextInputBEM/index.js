// Importing the style here still allows to import the component without the style,
// by importing the component without going through this index.js file
import './textInput.css'

export {default} from './TextInputBEM'
